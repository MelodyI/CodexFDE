# ImageGen 校正提示词

## 校正 1

Edit this teaching infographic only to correct the central inventory visualization. Preserve all other panels, text, typography and composition. In the panel 开发前：先约定 REMOVE ALL cardboard box drawings (there are currently 11, which is wrong). Replace the boxes with three simple vertically stacked large text badges: “现有：10 件” then “订单占用：3 件” then “还能卖：7 件”. Include one small equation 10 − 3 = 7. No countable objects or extra stock numbers. Preserve the sentence below 这组数据，应该显示 7 件。 All other content unchanged. Opaque white background.

## 校正 2

Edit this teaching diagram: ensure the ENTIRE canvas is solid opaque white, not transparent or black. Clean antialiasing, all navy text readable. Preserve positions, all Chinese labels, actors, stock table, outer arrows and footer. Fix the interior workflow only: three paper cards labelled 要求, 任务, 检查结果 should form a compact group labelled by existing card names. Remove the old arrows between the paper cards. Draw clear rightward arrow from developer toward the Codex robot ABOVE the cards labelled “交代任务”; draw leftward arrow from robot back toward developer BELOW the cards labelled “返回结果”. Keep all other external arrows and labels. Ensure no crossing arrows, no overlapping labels and no new jargon. Flat opaque white background everywhere.
