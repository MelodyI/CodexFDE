# ImageGen 配图修订记录

2026-09-19。全部为教学插图，不是产品截图或实测结果。各图原始提示词见同名 `.prompt.md`。

- `04-async.png`：通过 ImageGen 局部编辑，删除上方多余的绿色回指箭头；数据库到 202 响应仅保留终点向上箭头，消除双向响应误读。
- `02-identities.png` 与 `06-state-evidence.png`：通过 ImageGen 局部编辑，把“候选”旁的人物图标换成代码图标。候选在此指软件版本，不是人员。

编辑提示词：

> Edit this educational illustration minimally. Keep all objects, Chinese labels, placement, color and white background unchanged. Correct green return-response arrows: delete the entire horizontal green dashed arrow between the upper submission envelope and upper 202 envelope. On the L-shaped green dashed path labelled 返回 202, remove the downward arrowhead near the database. The path must START at 保存任务 database with no arrowhead there, then go up and right, and END with ONE upward arrowhead at 收到 202 与 T-001 envelope. It is one-way database to response. Keep all blue arrows unchanged. No other changes.

> Make one minimal semantic correction to this teaching illustration: the icon accompanying 候选 is currently a human silhouette / people. Replace ONLY that people icon with a blue software code symbol </> or code document icon, because 候选 means a candidate software version, not a person. Keep every Chinese text character, all objects, positions, arrows, sizes, colors and white background unchanged. Do not alter any other icons.
