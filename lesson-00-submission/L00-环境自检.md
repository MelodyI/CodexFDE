# L00 环境自检记录

> 由空白模板 `docs/courses/labs/L00/SUBMISSION.md` 另存而来。本文件为提交记录；`docs/courses/labs/L00/SUBMISSION.md` 后续由本人另填，二者内容以本文件为准。
> 第 2 节与第 3 节为 2026-09-25 本机实际运行所得；第 4～6 节需要本人亲自判断与操作，尚未填写（按“未完成”处理）。

## 1. 我的准备状态

- 学习署名：施成业
- 记录日期：2026-09-25
- 提交入口／截止时间（从开营通知抄录；未收到写“待确认”）：待确认
- 当前状态：需要辅导（工具可用，但 Python 版本与课堂统一要求不一致）
- 停在检查点：B 环境
- 本次需要老师帮助的一个问题：仓库内 `.venv` 的 Python 是 3.14.7，课堂统一使用 3.11.x；是否需要用 3.11 重建 `.venv`，还是可以直接沿用 3.14.7？

## 2. 我的环境

| 项目                                | 我的实际信息                    |
| ---------------------------------- | ----------------------------- |
| 操作系统及版本                       | macOS 15.7.5                  |
| 终端类型                            | zsh                           |
| Python                             | 3.14.7                        |
| 实际 Python 解释器路径               | `/opt/homebrew/bin/python3`   |
| Git 版本                           | 2.50.1                        |
| VS Code 版本及取得方式               | 1.139.0                       |
| Codex CLI 版本                     | 0.156.1                       |
| Codex 桌面入口是否能登录并打开目录     |                               |
| 课程仓库绝对路径                     | `./CodexFDE`                  |
| .venv 解释器路径                    | `./CodexFDE/.venv/bin/python` |
| SQLite 版本                        | 3.53.4                        |

## 3. 原始检查记录

将每项实际命令与输出粘贴在相应位置。退出码应紧接被检查的外部命令读取；来不及记录就重新检查，不猜测。敏感信息适当遮盖。

### Python、解释器路径与 SQLite

```text
$ .venv/bin/python -X utf8 -c "import sys, sqlite3; print(sys.version); print(sys.executable); print('sqlite', sqlite3.sqlite_version)"
3.14.7 (main, Aug  5 2026, 10:29:49) [Clang 17.0.0 (clang-1700.6.4.2)]
/Users/leaf.shi/career/study/2026/codex/sourcecode/CodexFDE/.venv/bin/python
sqlite 3.53.4
$ echo $?
0
```

逐项核对结论：Python 版本 **不是** 3.11.x（第 2 步“课堂版本正确”一项不通过）；解释器路径包含本仓库 `.venv`（通过）；SQLite 可读（通过）。

### 课程包导入（imports-ok）

先 `cd` 到课程根目录，再整行复制执行：

```bash
.venv/bin/python -X utf8 -c "import workbench, eval; print('imports-ok')"; echo "exit=$?"
```

2026-09-25 实测输出：

```text
imports-ok
exit=0
```

### workbench.cli --help

```bash
.venv/bin/python -X utf8 -m workbench.cli --help; echo "exit=$?"
```

2026-09-25 实测输出（子命令说明略，完整清单见终端）：

```text
usage: python -m workbench.cli [-h]
                               {workbench-init,workbench-project-add,...,course-*,task-*,init,backup,doctor,...} ...

个人研发工作台 CLI。工作台组织任务、授权与复验；Codex 是开发伙伴；FlowERP 是持续交付的客户产品。

（子命令说明略，含 workbench-init、environment-check、serve、serve-workbench、course-* 等）

命令分组
  工作台: serve-workbench, course-*, task-*, spec, feedback
  客户项目: serve, demo, mock-data, verify-mock-data
  可选平台（非大纲通过项）: harness-serve, harness-bootstrap

exit=0
```

### Git 状态与修改摘要

```bash
git status --short; echo "exit=$?"
git diff --stat; echo "exit=$?"
```

2026-09-25 实测输出：

```text
 M README.md
 M docs/courses/labs/L00/SUBMISSION.md
 M tests/test_desktop.py
 M workbench/desktop.py
?? learn/
?? lesson-00-submission/

exit=0
 README.md                           |  12 +++-
 docs/courses/labs/L00/SUBMISSION.md |  16 ++---
 tests/test_desktop.py               | 105 +++++++++++++++++++++++++--
 workbench/desktop.py                | 139 ++++++++++++++++++++++++++++++------
 4 files changed, 235 insertions(+), 37 deletions(-)
exit=0
```

- 是否误运行本讲范围外的命令：否（本讲未运行 `serve`、`demo`、`course-status`、全量测试或 blocking Eval）
- 若是，实际命令、结果及求助处理：不适用

## 4. Codex 只读回答与人工核验

> 未完成：需在 Codex 桌面或 CLI 中发出第 7 步的只读问题，并亲自打开两个文件核对。

- 使用的入口：桌面 / CLI（尚未确认）
- 只读问题与回答摘要：（尚未完成）

### 第一项
```bash
codex exec -C /Users/leaf.shi/career/study/2026/codex/sourcecode/CodexFDE -s read-only '
请只读检查当前课程仓库，不修改任何文件，也不运行会改变状态的命令。

请告诉我：
1. 仓库根目录路径是什么；
2. Python 最低版本写在哪个文件中；
3. 课程大纲保存在哪个文件中；
4. L01 开始时，为什么不能假设个人工作台已经存在；
5. 哪些结论无法仅凭当前文件确认。

请给出可定位的文件路径，不要编造。'
```

```text
1. **仓库根目录：**`/Users/leaf.shi/career/study/2026/codex/sourcecode/CodexFDE`。这是当前检查所在的工作目录。

2. **Python 最低版本：**写在 [`pyproject.toml`](/Users/leaf.shi/career/study/2026/codex/sourcecode/CodexFDE/pyproject.toml:10)：`requires-python = ">=3.10"`。仓库的 [`README.md`](/Users/leaf.shi/career/study/2026/codex/sourcecode/CodexFDE/README.md:137) 也说明最低要求为 Python 3.10，课堂统一使用 3.11.x。

3. **课程大纲：**保存在 [`docs/课程大纲-Codex-FDE行动营-个人研发自动化工作台.md`](/Users/leaf.shi/career/study/2026/codex/sourcecode/CodexFDE/docs/课程大纲-Codex-FDE行动营-个人研发自动化工作台.md)。

4. **L01 不能假设个人工作台已存在：**课程大纲明确规定，开课时没有个人工作台、工作台 Spec 或验收测试；L01 要从模糊诉求和真实痛点开始，由学生与 Codex 形成首份 Spec、验收测试并开发工作台第一块能力。见大纲第 29 行和 L01 学习弧表第 204 行。也就是说，工作台是 L01 起逐步构建的课程成果，不是 L01 的先决条件。

5. **仅凭当前仓库文件无法确认的结论：**文件能说明课程如何设计、仓库声明了哪些要求，但不能证明真实学员开课时的环境或起点、某位学生是否实际完成了 L01、学生是否亲自完成了 Spec 和红绿测试，也不能证明课程的实际教学效果。这些需要相应的环境记录、学生作品和教学数据支持。

本次只读检查没有修改文件，也没有运行状态可能改变的命令。
```

| 核验项 | Codex 的结论 | 我打开的文件和具体位置 | 支持该结论的内容 | 一致 / 不一致 / 未确认 |
|---|---|---|---|---|
| 第一项 | 未完成 | 未完成 | 未完成 | 未确认 |
| 第二项 | 未完成 | 未完成 | 未完成 | 未确认 |

## 5. 我的四个练习

> 未完成：练习 1～4 与变化题需要本人先写首次预测、再亲手运行，不得代写。

| 练习 | 独立完成 / 提示后完成 / 未完成 | 我的动作、结果与解释 | 使用了什么提示 |
|---|---|---|---|
| 1 找到目录与版本声明 | 未完成 | | |
| 2 先预测再运行算式 | 未完成 | | |
| 3 故意失败与修正计算 | 未完成 | | |
| 4 核验引用、理解检查范围 | 未完成 | | |

### 练习 3 的前后记录

- 失败时的完整命令：
- 实际错误与退出码：
- 修正后的完整命令：
- 实际输出与退出码：
- 哪一部分改变了，哪一部分保持不变：
- 为什么这不能直接证明 FlowERP 已完成：

### 变化题：30 件商品，9 件已留给客户

- 我的首次预测：
- 我运行的算式命令与实际输出：
- 找不到 `.venv` 中的 Python 时，我先检查什么、为什么：
- 出现 `AssertionError` 时，我先检查什么、为什么：
- 同伴／老师反馈（尚无反馈可留空）：
- 我据此修订了什么：

## 6. 带到 L01

- 我的一个真实痛点：
- 我希望工作台帮助我记住或组织什么：
- 我仍想问的一个问题：
- 其他未完成项与下一步：完成第 4～6 节；先解决 Python 3.11.x 与本仓 `.venv` 3.14.7 的版本差异

## 7. 提交前自查

- [x] 记录来自我的实际操作，未用示例替代。（第 2、3 节为 2026-09-25 本机实测）
- [x] 失败、提示和未完成项如实保留。（Python 版本不一致、Codex 桌面入口未确认、第 4～6 节未完成）
- [ ] 两个文件已人工核验，或明确标注尚未完成。
- [ ] 四个练习与变化题有自己的回答。
- [x] 已检查敏感信息，未附运行数据库或密钥。
- [ ] 文件保存后能重新打开。
